#include<bits/stdc++.h>
 using namespace std;

int main(){
 	int n,k,sz,i=0;
 	scanf("%d %d",&n,&k);
 	cin.ignore();
 	string s,s1;
 	cin>>s;
 	s1=s;
	while(k>0){
		for( ; i < n ; i++ ){
		 if(s[i]=='a'){
		 	s.erase(s.begin()+i,s.begin()+i+1);
		 	k--;
		 	break;
		 }
		}
		if(i==n)break;
	}
	i=0;
	while(k>0){
		for( ; i < n ; i++ ){
		 if(s[i]=='b'){
		 	s.erase(s.begin()+i,s.begin()+i+1);
		 	k--;
		 	break;
		 }
		}
		if(i==n)break;
	}
	i=0;
	while(k>0){
		for( ; i < n ; i++ ){
		 if(s[i]=='c'){
		 	s.erase(s.begin()+i,s.begin()+i+1);
		 	k--;
		 	break;
		 }
		}
		if(i==n)break;
	}
	while(k>0){
		for( ; i < n ; i++ ){
		 if(s[i]=='d'){
		 	s.erase(s.begin()+i,s.begin()+i+1);
		 	k--;
		 	break;
		 }
		}
		if(i==n)break;
	}
	while(k>0){
		for( ; i < n ; i++ ){
		 if(s[i]=='e'){
		 	s.erase(s.begin()+i,s.begin()+i+1);
		 	k--;
		 	break;
		 }
		}
		if(i==n)break;
	}
	while(k>0){
		for( ; i < n ; i++ ){
		 if(s[i]=='f'){
		 	s.erase(s.begin()+i,s.begin()+i+1);
		 	k--;
		 	break;
		 }
		}
		if(i==n)break;
	}
	while(k>0){
		for( ; i < n ; i++ ){
		 if(s[i]=='g'){
		 	s.erase(s.begin()+i,s.begin()+i+1);
		 	k--;
		 	break;
		 }
		}
		if(i==n)break;
	}
	while(k>0){
		for( ; i < n ; i++ ){
		 if(s[i]=='h'){
		 	s.erase(s.begin()+i,s.begin()+i+1);
		 	k--;
		 	break;
		 }
		}
		if(i==n)break;
	}
	while(k>0){
		for( ; i < n ; i++ ){
		 if(s[i]=='i'){
		 	s.erase(s.begin()+i,s.begin()+i+1);
		 	k--;
		 	break;
		 }
		}
		if(i==n)break;
	}
	while(k>0){
		for( ; i < n ; i++ ){
		 if(s[i]=='j'){
		 	s.erase(s.begin()+i,s.begin()+i+1);
		 	k--;
		 	break;
		 }
		}
		if(i==n)break;
	}

	while(k>0){
		for( ; i < n ; i++ ){
		 if(s[i]=='k'){
		 	s.erase(s.begin()+i,s.begin()+i+1);
		 	k--;
		 	break;
		 }
		}
		if(i==n)break;
	}
	while(k>0){
		for( ; i < n ; i++ ){
		 if(s[i]=='l'){
		 	s.erase(s.begin()+i,s.begin()+i+1);
		 	k--;
		 	break;
		 }
		}
		if(i==n)break;
	}
	while(k>0){
		for( ; i < n ; i++ ){
		 if(s[i]=='m'){
		 	s.erase(s.begin()+i,s.begin()+i+1);
		 	k--;
		 	break;
		 }
		}
		if(i==n)break;
	}
	while(k>0){
		for( ; i < n ; i++ ){
		 if(s[i]=='n'){
		 	s.erase(s.begin()+i,s.begin()+i+1);
		 	k--;
		 	break;
		 }
		}
		if(i==n)break;
	}
	while(k>0){
		for( ; i < n ; i++ ){
		 if(s[i]=='o'){
		 	s.erase(s.begin()+i,s.begin()+i+1);
		 	k--;
		 	break;
		 }
		}
		if(i==n)break;
	}
	while(k>0){
		for( ; i < n ; i++ ){
		 if(s[i]=='p'){
		 	s.erase(s.begin()+i,s.begin()+i+1);
		 	k--;
		 	break;
		 }
		}
		if(i==n)break;
	}
	while(k>0){
		for( ; i < n ; i++ ){
		 if(s[i]=='q'){
		 	s.erase(s.begin()+i,s.begin()+i+1);
		 	k--;
		 	break;
		 }
		}
		if(i==n)break;
	}
	while(k>0){
		for( ; i < n ; i++ ){
		 if(s[i]=='r'){
		 	s.erase(s.begin()+i,s.begin()+i+1);
		 	k--;
		 	break;
		 }
		}
		if(i==n)break;
	}
	while(k>0){
		for( ; i < n ; i++ ){
		 if(s[i]=='s'){
		 	s.erase(s.begin()+i,s.begin()+i+1);
		 	k--;
		 	break;
		 }
		}
		if(i==n)break;
	}
	while(k>0){
		for( ; i < n ; i++ ){
		 if(s[i]=='t'){
		 	s.erase(s.begin()+i,s.begin()+i+1);
		 	k--;
		 	break;
		 }
		}
		if(i==n)break;
	}
	while(k>0){
		for( ; i < n ; i++ ){
		 if(s[i]=='u'){
		 	s.erase(s.begin()+i,s.begin()+i+1);
		 	k--;
		 	break;
		 }
		}
		if(i==n)break;
	}
	while(k>0){
		for( ; i < n ; i++ ){
		 if(s[i]=='v'){
		 	s.erase(s.begin()+i,s.begin()+i+1);
		 	k--;
		 	break;
		 }
		}
		if(i==n)break;
	}
	while(k>0){
		for( ; i < n ; i++ ){
		 if(s[i]=='w'){
		 	s.erase(s.begin()+i,s.begin()+i+1);
		 	k--;
		 	break;
		 }
		}
		if(i==n)break;
	}
	while(k>0){
		for( ; i < n ; i++ ){
		 if(s[i]=='x'){
		 	s.erase(s.begin()+i,s.begin()+i+1);
		 	k--;
		 	break;
		 }
		}
		if(i==n)break;
	}
	while(k>0){
		for( ; i < n ; i++ ){
		 if(s[i]=='y'){
		 	s.erase(s.begin()+i,s.begin()+i+1);
		 	k--;
		 	break;
		 }
		}
		if(i==n)break;
	}
while(k>0){
		for( ; i < n ; i++ ){
		 if(s[i]=='z'){
		 	s.erase(s.begin()+i,s.begin()+i+1);
		 	k--;
		 	break;
		 }
		}
		if(i==n)break;
	}
	cout<<s;
 }